apiVersion: v1
kind: Service
metadata:
  name: todo-app-service
spec:
  selector:
    app: todo-app
  ports:
    - protocol: TCP
      port: 80        # The port the cluster exposes
      targetPort: 8000 # The port our container listens on
  type: LoadBalancer